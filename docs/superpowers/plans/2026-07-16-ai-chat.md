# AI Chat (`@component-ai/ai-chat`) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新建 React 包 `@component-ai/ai-chat`：OpenAI 兼容流式 `useChat` + `Chat`/`MessageList`/`Message`/`Composer`，支持代理与可选 `apiKey`，Storybook 可用 mock transport 演示完整一轮对话（含 stop / retry / error）。

**Architecture:** 单包内部分层——`src/core/`（纯 TS：类型、SSE 解析、`openaiTransport`）+ `useChat` + UI 组件。不做独立 `chat-core` 包、不做 Vue、不做 Markdown。按钮优先用 `@component-ai/react-ui` 的 `Button`（optional peer），未安装时用本地 fallback。

**Tech Stack:** TypeScript、React 19、Vite 8、vite-plugin-dts、Vitest、Testing Library、Tailwind v4、Storybook 8

**规格来源:** [`docs/superpowers/specs/2026-07-16-ai-chat-design.md`](../specs/2026-07-16-ai-chat-design.md)

---

## 文件结构

| 路径 | 职责 |
|------|------|
| `packages/ai-chat/package.json` | 包元数据、peer、scripts |
| `packages/ai-chat/tsconfig.json` | 严格 TS（含 DOM，因 fetch/stream） |
| `packages/ai-chat/vite.config.ts` | lib ESM + dts；external react |
| `packages/ai-chat/vitest.config.ts` | 默认 jsdom；core 测试可用 node |
| `packages/ai-chat/src/style.css` | `@import "tailwindcss"` |
| `packages/ai-chat/src/core/types.ts` | 消息与 transport 类型 |
| `packages/ai-chat/src/core/id.ts` | `createMessageId` |
| `packages/ai-chat/src/core/parse-sse.ts` | 从 SSE 文本块提取 delta content |
| `packages/ai-chat/src/core/openai-transport.ts` | 默认 transport |
| `packages/ai-chat/src/core/*.test.ts` | core 单测 |
| `packages/ai-chat/src/useChat.ts` | hook |
| `packages/ai-chat/src/useChat.test.tsx` | hook 测试 |
| `packages/ai-chat/src/components/Chat.tsx` | 根 + context |
| `packages/ai-chat/src/components/MessageList.tsx` | 列表 |
| `packages/ai-chat/src/components/Message.tsx` | 气泡 |
| `packages/ai-chat/src/components/Composer.tsx` | 输入区 |
| `packages/ai-chat/src/components/ChatButton.tsx` | Button 或 fallback |
| `packages/ai-chat/src/components/*.test.tsx` | UI 测试 |
| `packages/ai-chat/src/components/Chat.stories.tsx` | Storybook |
| `packages/ai-chat/src/index.ts` | 导出 |
| `packages/ai-chat/README.md` | 代理 vs apiKey（P2 文档最低限度） |
| 根 `package.json` | `build:ai-chat` / `test:ai-chat` |
| `apps/storybook-react/...` | 增加 stories glob + 依赖 |

**锁定约定**

- 默认 `model`: `"gpt-4o-mini"`
- `createMessageId`: 模块内递增 `msg_${n}`（测试可预测；非 `crypto.randomUUID`）
- `stop`：abort 后 assistant 保留文本，`status: "done"`，hook → `idle`
- `retry`：仅最后一条 assistant 为 `error` 时可用
- `reload`：删掉最后一条 assistant（若存在），基于最后一条 user 再请求

---

### Task 1: 搭建 `@component-ai/ai-chat` 包骨架

**Files:**
- Create: `packages/ai-chat/package.json`
- Create: `packages/ai-chat/tsconfig.json`
- Create: `packages/ai-chat/vite.config.ts`
- Create: `packages/ai-chat/vitest.config.ts`
- Create: `packages/ai-chat/src/style.css`
- Create: `packages/ai-chat/src/index.ts`
- Modify: 根 `package.json`

- [ ] **Step 1: package.json**

```json
{
  "name": "@component-ai/ai-chat",
  "version": "1.0.0",
  "type": "module",
  "description": "React AI chat: useChat + OpenAI-compatible streaming UI",
  "license": "ISC",
  "sideEffects": ["**/*.css"],
  "files": ["dist"],
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./style.css": "./dist/style.css"
  },
  "peerDependencies": {
    "react": "^18.0.0 || ^19.0.0",
    "react-dom": "^18.0.0 || ^19.0.0",
    "@component-ai/react-ui": "^1.0.0"
  },
  "peerDependenciesMeta": {
    "@component-ai/react-ui": { "optional": true }
  },
  "devDependencies": {
    "@component-ai/react-ui": "1.0.0",
    "@tailwindcss/vite": "^4.1.17",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.1",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.1",
    "jsdom": "^29.1.1",
    "react": "^19.2.6",
    "react-dom": "^19.2.6",
    "tailwindcss": "^4.1.17",
    "typescript": "^6.0.3",
    "vite": "^8.0.10",
    "vite-plugin-dts": "^5.0.0",
    "vitest": "^4.1.5"
  },
  "scripts": {
    "build": "vite build",
    "test": "vitest run",
    "test:watch": "vitest"
  }
}
```

- [ ] **Step 2: tsconfig / vite / vitest / style**

`tsconfig.json`：对齐 `react-ui`（`jsx: react-jsx`，`lib: ["ES2022", "DOM", "DOM.Iterable"]`）。

`vite.config.ts`：对齐 `react-ui`，`name: "ComponentAiAiChat"`，`external`: `react`、`react-dom`、`react/jsx-runtime`、`@component-ai/react-ui`。

`vitest.config.ts`：

```ts
import react from "@vitejs/plugin-react";
import { defineConfig } from "vitest/config";

export default defineConfig({
  plugins: [react()],
  test: {
    environment: "jsdom",
    setupFiles: [],
    include: ["src/**/*.test.{ts,tsx}"],
  },
});
```

`src/style.css`:

```css
@import "tailwindcss";
```

`src/index.ts`:

```ts
import "./style.css";
export {};
```

根 scripts 增加：

```json
"build:ai-chat": "npm run build -w @component-ai/ai-chat",
"test:ai-chat": "npm run test -w @component-ai/ai-chat"
```

- [ ] **Step 3: 安装**

Run: `npm install`  
Expected: workspace 识别 `@component-ai/ai-chat`

- [ ] **Step 4: Commit**

```bash
git add packages/ai-chat package.json package-lock.json
git commit -m "chore(ai-chat): scaffold React package with Vite and Vitest"
```

---

### Task 2: Core 类型 + `createMessageId` + `parseSseDelta`

**Files:**
- Create: `packages/ai-chat/src/core/types.ts`
- Create: `packages/ai-chat/src/core/id.ts`
- Create: `packages/ai-chat/src/core/parse-sse.ts`
- Create: `packages/ai-chat/src/core/parse-sse.test.ts`
- Modify: `packages/ai-chat/src/index.ts`

- [ ] **Step 1: 失败测试**

Create `packages/ai-chat/src/core/parse-sse.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { extractDeltaFromSseChunk } from "./parse-sse";

describe("extractDeltaFromSseChunk", () => {
  it("extracts concatenated delta contents from data lines", () => {
    const chunk = [
      'data: {"choices":[{"delta":{"content":"Hel"}}]}',
      "",
      'data: {"choices":[{"delta":{"content":"lo"}}]}',
      "",
      "data: [DONE]",
      "",
    ].join("\n");
    expect(extractDeltaFromSseChunk(chunk)).toBe("Hello");
  });

  it("ignores lines without content delta", () => {
    const chunk = 'data: {"choices":[{"delta":{}}]}\n\n';
    expect(extractDeltaFromSseChunk(chunk)).toBe("");
  });

  it("skips malformed JSON lines", () => {
    const chunk = "data: not-json\n\ndata: {\"choices\":[{\"delta\":{\"content\":\"x\"}}]}\n\n";
    expect(extractDeltaFromSseChunk(chunk)).toBe("x");
  });
});
```

- [ ] **Step 2: Run — expect RED**

Run: `npm run test -w @component-ai/ai-chat -- src/core/parse-sse.test.ts`  
Expected: FAIL

- [ ] **Step 3: 实现**

`types.ts`：按规格 §5.1 导出全部类型。

`id.ts`:

```ts
let seq = 0;
export function createMessageId(): string {
  seq += 1;
  return `msg_${seq}`;
}
/** @internal test helper */
export function resetMessageIdSeq(): void {
  seq = 0;
}
```

`parse-sse.ts`:

```ts
export function extractDeltaFromSseChunk(chunk: string): string {
  let out = "";
  for (const line of chunk.split(/\r?\n/)) {
    const trimmed = line.trim();
    if (!trimmed.startsWith("data:")) continue;
    const data = trimmed.slice(5).trim();
    if (!data || data === "[DONE]") continue;
    try {
      const json = JSON.parse(data) as {
        choices?: Array<{ delta?: { content?: string } }>;
      };
      const piece = json.choices?.[0]?.delta?.content;
      if (typeof piece === "string") out += piece;
    } catch {
      // skip malformed
    }
  }
  return out;
}
```

- [ ] **Step 4: GREEN + Commit**

Run: `npm run test -w @component-ai/ai-chat -- src/core/parse-sse.test.ts`  
Expected: PASS

```bash
git add packages/ai-chat/src/core
git commit -m "feat(ai-chat): add message types and SSE delta parser"
```

---

### Task 3: `openaiTransport`

**Files:**
- Create: `packages/ai-chat/src/core/openai-transport.ts`
- Create: `packages/ai-chat/src/core/openai-transport.test.ts`

- [ ] **Step 1: 失败测试**

用 `vi.stubGlobal("fetch", ...)` 返回 `ReadableStream`，编码若干 SSE chunk；断言 `onToken` 拼出全文、`onDone` 调用；另测非 2xx → `onError`；abort → 不抛未捕获（可用 `onError` 或静默结束，**锁定：abort 不调用 onError，直接 return，由 useChat 标 done**）。

最小用例：

```ts
it("streams tokens then calls onDone", async () => { /* mock fetch body */ });
it("calls onError on HTTP error", async () => { /* status 500 */ });
it("sends Authorization when apiKey set", async () => { /* assert headers */ });
```

- [ ] **Step 2: RED → 实现 `openaiTransport`**

```ts
export const openaiTransport: ChatTransport = {
  async stream(request, handlers) {
    const headers: Record<string, string> = {
      "Content-Type": "application/json",
      ...(request.headers ?? {}),
    };
    if (request.apiKey) {
      headers.Authorization = `Bearer ${request.apiKey}`;
    }
    let response: Response;
    try {
      response = await fetch(request.apiUrl, {
        method: "POST",
        headers,
        body: JSON.stringify({
          model: request.model,
          messages: request.messages,
          stream: true,
        }),
        signal: request.signal,
      });
    } catch (err) {
      if (request.signal?.aborted) return;
      handlers.onError(err instanceof Error ? err : new Error(String(err)));
      return;
    }
    if (!response.ok) {
      handlers.onError(new Error(`Chat request failed: ${response.status}`));
      return;
    }
    if (!response.body) {
      handlers.onError(new Error("Chat response missing body"));
      return;
    }
    const reader = response.body.getReader();
    const decoder = new TextDecoder();
    try {
      while (true) {
        const { done, value } = await reader.read();
        if (done) break;
        const text = extractDeltaFromSseChunk(decoder.decode(value, { stream: true }));
        if (text) handlers.onToken(text);
      }
      handlers.onDone();
    } catch (err) {
      if (request.signal?.aborted) return;
      handlers.onError(err instanceof Error ? err : new Error(String(err)));
    }
  },
};
```

- [ ] **Step 3: GREEN + Commit**

```bash
git add packages/ai-chat/src/core
git commit -m "feat(ai-chat): add OpenAI-compatible streaming transport"
```

---

### Task 4: `useChat`

**Files:**
- Create: `packages/ai-chat/src/useChat.ts`
- Create: `packages/ai-chat/src/useChat.test.tsx`
- Modify: `packages/ai-chat/src/index.ts`

- [ ] **Step 1: 失败测试（jsdom）**

覆盖：

1. `send("hi")` → messages 含 user + streaming assistant；mock transport `onToken`/`onDone` → content 更新、status idle  
2. `stop` → abort，assistant 保留已有文本且 `done`  
3. transport `onError` → assistant `error`，hook status `error`；`retry` 再成功  
4. `reload` 在成功一轮后再生成  

使用可控 mock transport（手动调用 handlers）。

- [ ] **Step 2: RED → 实现 `useChat`**

要点：`useRef` 持有 `AbortController`；`send` 防重入（streaming 中忽略）；发送后 `setInput("")`；`toApiMessages` 只映射已完成的 user/assistant（当前 streaming assistant 不进请求，或只进 user——**锁定：请求 messages = 历史中 status 非 error 的 user/assistant 的 role+content，加上本次 user，不含空 streaming assistant**）。

- [ ] **Step 3: GREEN + 导出 + Commit**

```ts
export { useChat } from "./useChat";
export type { UseChatOptions, UseChatReturn } from "./useChat";
export type { ChatMessage, ChatRole, ChatTransport } from "./core/types";
export { openaiTransport } from "./core/openai-transport";
```

```bash
git add packages/ai-chat/src
git commit -m "feat(ai-chat): add useChat with stream stop retry reload"
```

---

### Task 5: UI 组件 `Chat` / `MessageList` / `Message` / `Composer`

**Files:**
- Create: `packages/ai-chat/src/components/ChatButton.tsx`
- Create: `packages/ai-chat/src/components/Message.tsx`
- Create: `packages/ai-chat/src/components/MessageList.tsx`
- Create: `packages/ai-chat/src/components/Composer.tsx`
- Create: `packages/ai-chat/src/components/Chat.tsx`
- Create: `packages/ai-chat/src/components/Composer.test.tsx`
- Create: `packages/ai-chat/src/components/Chat.test.tsx`
- Modify: `packages/ai-chat/src/index.ts`

- [ ] **Step 1: ChatButton fallback**

动态 `import` 不可靠于同步渲染；用 try/catch 包一层可选依赖太脆。**锁定**：始终用本地 `ChatButton`，样式对齐 `react-ui` Button primary/secondary；若未来要复用，再改为可选。规格「优先复用」在 README 说明「视觉对齐 Button；本包自带轻量按钮以避免强制 peer 安装失败」。`peerDependencies` 中 `react-ui` 保持 optional，但不在运行时 import。

- [ ] **Step 2: 组件失败测试**

`Composer.test.tsx`：streaming 时主按钮 `aria-label="停止"`；idle 时 `发送`；error 时出现 `重试`。  
`Chat.test.tsx`：mock transport，输入发送后列表出现 user 文案与 assistant 流式结果。

- [ ] **Step 3: 实现组件**

- `Chat`：`useChat(props)` + `createContext`；children  
- `MessageList`：consume context 或 props.messages；`role="log"` `aria-live="polite"`；简单 `useEffect` scrollIntoView 跟随（用 ref；若 `scrollTop` 距底部 > 80px 则暂停跟随）  
- `Message`：user 右对齐 sky，assistant 左对齐 slate；streaming 显示 `…` 或闪烁光标 span  
- `Composer`：textarea/input + ChatButton  

Tailwind class 与现有库一致（`text-sm`、`rounded-lg`、`border-slate-300` 等）。

- [ ] **Step 4: GREEN + 导出 + Commit**

```bash
git add packages/ai-chat/src
git commit -m "feat(ai-chat): add Chat MessageList Message Composer UI"
```

---

### Task 6: Storybook + README + 构建冒烟

**Files:**
- Create: `packages/ai-chat/src/components/Chat.stories.tsx`
- Create: `packages/ai-chat/README.md`
- Modify: `apps/storybook-react/.storybook/main.mjs`
- Modify: `apps/storybook-react/package.json`

- [ ] **Step 1: Stories**

`title: "React/AIChat"`：

- `MockStream`：mock transport 每 50ms 吐字  
- `ErrorRetry`：首次失败第二次成功  
- `Stop`：长流式可点停止  

- [ ] **Step 2: 接入 Storybook**

`main.mjs` stories 增加：

```js
"../../../packages/ai-chat/src/**/*.stories.@(ts|tsx)",
```

`apps/storybook-react/package.json` dependencies 增加 `"@component-ai/ai-chat": "^1.0.0"`，然后 `npm install`。

- [ ] **Step 3: README（P2 最低限度）**

说明：`apiUrl` 代理 vs 浏览器 `apiKey` 风险；基本 `useChat` / `<Chat>` 示例。

- [ ] **Step 4: 构建与测试**

```bash
npm run test -w @component-ai/ai-chat
npm run build -w @component-ai/ai-chat
```

Expected: 全部通过。

- [ ] **Step 5: Commit**

```bash
git add packages/ai-chat apps/storybook-react package-lock.json
git commit -m "docs(ai-chat): add Storybook stories and README for proxy vs apiKey"
```

- [ ] **Step 6: 人工验证（勿由 agent 勾选）**

`npm run storybook:react` → 打开 React/AIChat 三条 story。

---

## Self-review（对照规格）

| 规格项 | Task |
|--------|------|
| 单包分层 core/hook/UI | 1–5 |
| OpenAI SSE + apiKey/headers | 3 |
| useChat send/stop/retry/reload | 4 |
| Chat UI + a11y | 5 |
| Storybook mock + README | 6 |
| 无 Markdown / 无 Vue / 无独立 chat-core | 遵守 |

---

## 完成定义

- `npm run test -w @component-ai/ai-chat` 通过  
- `npm run build -w @component-ai/ai-chat` 成功  
- Storybook 可加载 AIChat stories（人工确认）  
