# Transfer（穿梭框）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 双端交付独立 `Transfer`：左右分栏、Checkbox 勾选、Button 搬移、`targetKeys` 受控/非受控。

**Architecture:** 只做 UI 胶水。用已导出的 `Checkbox` 与 `Button`。按 `targetKeys` 把 `dataSource` 切成左右栏；面板勾选是内部 state。不新增 core 包。

**Tech Stack:** TypeScript、React 19、Vue 3.5 TSX、Vitest、Testing Library / Vue Test Utils、Tailwind v4

**规格来源:** [`docs/superpowers/specs/2026-09-09-transfer-design.md`](../specs/2026-09-09-transfer-design.md)

## Global Constraints

- 受控优先：`targetKeysProp ?? uncontrolled`（Vue：`modelValue ?? internal`）
- 变更一律回调，不管受控与否
- Vue：`defineComponent` + TSX；`PropType`；`emits` + `emit`
- 禁止自绘 checkbox / 原生 button 冒充依赖件
- TDD：RED 再 GREEN；agent 可勾选自动化步骤；勿勾选 Storybook 目视与 git commit

---

## 文件结构

| 路径 | 职责 |
|------|------|
| `packages/react-ui/src/components/Transfer.tsx` | React Transfer |
| `packages/react-ui/src/components/Transfer.test.tsx` | React 测试 |
| `packages/react-ui/src/components/Transfer.stories.tsx` | React Story |
| `packages/vue-ui/src/components/Transfer.tsx` | Vue Transfer |
| `packages/vue-ui/src/components/Transfer.test.tsx` | Vue 测试 |
| `packages/vue-ui/src/components/Transfer.stories.tsx` | Vue Story |
| `packages/*/src/index.ts` | 导出 `Transfer`、`TransferItem` |
| `AGENTS.md` | 组件表增加 Transfer；纠正 VirtualGrid / Form 状态 |

共享测试数据：

```ts
const ITEMS = [
  { key: "a", title: "Alpha" },
  { key: "b", title: "Beta" },
  { key: "c", title: "Gamma", disabled: true },
  { key: "d", title: "Delta" },
];
```

---

### Task 1: React Transfer

**Files:**
- Create: `packages/react-ui/src/components/Transfer.test.tsx`
- Create: `packages/react-ui/src/components/Transfer.tsx`
- Create: `packages/react-ui/src/components/Transfer.stories.tsx`
- Modify: `packages/react-ui/src/index.ts`

- [ ] **Step 1: 写失败测试**

```tsx
import { describe, expect, it, vi } from "vitest";
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { Transfer } from "./Transfer";

const ITEMS = [
  { key: "a", title: "Alpha" },
  { key: "b", title: "Beta" },
  { key: "c", title: "Gamma", disabled: true },
  { key: "d", title: "Delta" },
];

describe("Transfer (React)", () => {
  it("puts defaultTargetKeys on the right panel", () => {
    render(
      <Transfer dataSource={ITEMS} defaultTargetKeys={["b"]} titles={["源", "目标"]} />,
    );
    const source = screen.getByRole("listbox", { name: "源" });
    const target = screen.getByRole("listbox", { name: "目标" });
    expect(source).toHaveTextContent("Alpha");
    expect(source).not.toHaveTextContent("Beta");
    expect(target).toHaveTextContent("Beta");
  });

  it("moves checked source items to the right and calls onTargetKeysChange", async () => {
    const user = userEvent.setup();
    const onTargetKeysChange = vi.fn();
    render(
      <Transfer
        dataSource={ITEMS}
        defaultTargetKeys={[]}
        onTargetKeysChange={onTargetKeysChange}
        titles={["源", "目标"]}
      />,
    );
    await user.click(screen.getByRole("checkbox", { name: "Alpha" }));
    await user.click(screen.getByRole("button", { name: "移到右侧" }));
    expect(onTargetKeysChange).toHaveBeenCalledWith(["a"]);
    expect(screen.getByRole("listbox", { name: "目标" })).toHaveTextContent("Alpha");
  });

  it("does not move disabled items and keeps controlled targetKeys", async () => {
    const user = userEvent.setup();
    const onTargetKeysChange = vi.fn();
    render(
      <Transfer
        dataSource={ITEMS}
        targetKeys={[]}
        onTargetKeysChange={onTargetKeysChange}
        titles={["源", "目标"]}
      />,
    );
    const gamma = screen.getByRole("checkbox", { name: "Gamma" });
    expect(gamma).toBeDisabled();
    await user.click(gamma);
    await user.click(screen.getByRole("button", { name: "移到右侧" }));
    expect(onTargetKeysChange).not.toHaveBeenCalled();
    expect(screen.getByRole("listbox", { name: "源" })).toHaveTextContent("Gamma");
  });

  it("moves target items back to the left", async () => {
    const user = userEvent.setup();
    const onTargetKeysChange = vi.fn();
    render(
      <Transfer
        dataSource={ITEMS}
        defaultTargetKeys={["a", "b"]}
        onTargetKeysChange={onTargetKeysChange}
        titles={["源", "目标"]}
      />,
    );
    await user.click(screen.getByRole("checkbox", { name: "Alpha" }));
    await user.click(screen.getByRole("button", { name: "移到左侧" }));
    expect(onTargetKeysChange).toHaveBeenCalledWith(["b"]);
    expect(screen.getByRole("listbox", { name: "源" })).toHaveTextContent("Alpha");
  });

  it("check-all selects enabled items in a panel", async () => {
    const user = userEvent.setup();
    render(
      <Transfer dataSource={ITEMS} defaultTargetKeys={[]} titles={["源", "目标"]} />,
    );
    const source = screen.getByRole("listbox", { name: "源" }).parentElement!;
    const checkAll = source.querySelector('input[type="checkbox"]') as HTMLInputElement;
    await user.click(checkAll);
    expect(screen.getByRole("checkbox", { name: "Alpha" })).toBeChecked();
    expect(screen.getByRole("checkbox", { name: "Gamma" })).not.toBeChecked();
  });
});
```

- [ ] **Step 2: RED** — `npm run test -w @component-ai/react-ui -- src/components/Transfer.test.tsx`  
  期望：无法解析 `./Transfer` 或测试失败。

- [ ] **Step 3: 实现 `Transfer.tsx`**

要点：

- `targetKeys = targetKeysProp ?? uncontrolled`（`useState(defaultTargetKeys)`）
- `split(dataSource, keys)` 保持 `dataSource` 顺序
- 每栏内部 `Set<string>` 勾选；全选 Checkbox `indeterminate`
- 搬移：过滤 disabled；`onTargetKeysChange(next)`；非受控则 `setUncontrolled(next)`；清空被搬走的勾选
- 使用 `Checkbox`、`Button variant="secondary"`
- 根 `role="group"` `aria-label="穿梭框"`；listbox 的 `aria-label` 为该栏 title

- [ ] **Step 4: GREEN** — 同上测试命令，全部通过

- [ ] **Step 5: Story + 导出**

`Transfer.stories.tsx`：`title: 'React/Transfer'`，`Basic`（`defaultTargetKeys={['b']}`）、`Controlled`、`DisabledItems`（含 `c` disabled）。

`index.ts` 增加：

```ts
export { Transfer } from "./components/Transfer";
export type { TransferProps, TransferItem } from "./components/Transfer";
```

- [ ] **Step 6: `npm run test -w @component-ai/react-ui` 与 `npm run build -w @component-ai/react-ui`**

---

### Task 2: Vue Transfer

**Files:**
- Create: `packages/vue-ui/src/components/Transfer.test.tsx`
- Create: `packages/vue-ui/src/components/Transfer.tsx`
- Create: `packages/vue-ui/src/components/Transfer.stories.tsx`
- Modify: `packages/vue-ui/src/index.ts`

- [ ] **Step 1: 镜像失败测试**（`modelValue` / `defaultModelValue` / `update:modelValue`；`mount` + `trigger('click')` 在 checkbox 与 `button[aria-label="移到右侧"]`）

断言：`emitted()['update:modelValue']` 为 `[['a']]` 这类 payload。

- [ ] **Step 2: RED** — `npm run test -w @component-ai/vue-ui -- src/components/Transfer.test.tsx`

- [ ] **Step 3: `defineComponent` + TSX 实现**，语义对齐 Task 1

- [ ] **Step 4: GREEN**

- [ ] **Step 5: Story `title: 'Vue/Transfer'` + `index.ts` 导出 `Transfer` 与类型

- [ ] **Step 6: `npm run test -w @component-ai/vue-ui` 与 `npm run build -w @component-ai/vue-ui`**

---

### Task 3: AGENTS.md 状态校正

**Files:**
- Modify: `AGENTS.md`

- [ ] 组件表：补 `Transfer` ✅ / ✅；`Form` / `Input` / `Radio` / `Switch` / `ai-chat` 按仓库实际勾选
- [ ] VirtualGrid 行改为 P0–P5 已完成（代码在 master），P3 Form 嵌套路径见 PR
- [ ] 结构树补 `form-core`、`ai-chat`

---

## Self-review

- In scope: 双端 Transfer、受控/非受控、disabled 项、全选、Story、导出、AGENTS.md
- Out of scope: 搜索、分页、拖拽、Form 接线 Story、独立 core 包
- Spec §4–§7 均有对应 Task 1/2 测试或实现要点

## 完成定义

双端可构建、Transfer 单测通过、Story 可打开。目视与 commit 步骤不由 plan checkbox 代替。
